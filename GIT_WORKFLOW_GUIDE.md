# دليل تجنب مشاكل Merge Conflicts 🔄

## لماذا تحدث مشاكل Merge Conflicts بشكل متكرر؟ 🤔

### الأسباب الرئيسية:

1. **ملفات `index.js` البسيطة:**
   - هذه الملفات تحتوي فقط على سطر واحد: `export { default } from './ComponentName';`
   - عندما يعدل كل منك وصديقك نفس السطر (حتى لو كان نفس الكود)، يحدث conflict
   - إضافة مسافات فارغة أو أسطر فارغة يمكن أن تسبب conflicts أيضاً

2. **عدم المزامنة المستمرة:**
   - عندما تعمل أنت وصديقك على نفس المشروع لفترة طويلة بدون merge
   - كلما طالت المدة، زادت احتمالية حدوث conflicts

3. **تعديل نفس الملفات:**
   - إذا عدلتم نفس الملفات في نفس الوقت، خاصة الملفات المشتركة

## الحلول والوقاية ⚡

### 1. استراتيجية Git Workflow أفضل:

```bash
# قبل البدء بالعمل، دائماً:
git pull origin main    # أو master حسب اسم الفرع الرئيسي

# أثناء العمل، اجعل commits صغيرة ومتكررة:
git add .
git commit -m "وصف التغيير"
git push origin your-branch

# قبل merge، دائماً:
git pull origin main --rebase
# أو
git pull origin main
```

### 2. تقسيم العمل (Work Division):

✅ **افعلوا هذا:**
- اتفقوا مسبقاً على من يعمل على أي جزء من المشروع
- استخدموا قنوات التواصل (Slack, Discord, etc.) لتنسيق العمل
- احتفظوا بقائمة بالمهام ووزعوها

❌ **لا تفعلوا هذا:**
- لا تعملوا على نفس الملف في نفس الوقت
- لا تعدلوا ملفات `index.js` إلا عند الضرورة

### 3. استخدام Git Hooks (اختياري):

يمكنك إعداد pre-commit hook لتنظيف الأسطر الفارغة في ملفات `index.js`:

```bash
# .git/hooks/pre-commit
#!/bin/bash
find src -name "index.js" -type f -exec sed -i '' -e :a -e '/^\n*$/{$d;N;ba' -e '}' {} \;
```

### 4. عند حدوث Conflict:

#### الطريقة الصحيحة لحل Conflicts:

1. **استخدم VS Code:**
   - افتح الملف الذي به conflict
   - VS Code يعرض خيارات:
     - "Accept Current Change" (قبول تغييراتك)
     - "Accept Incoming Change" (قبول تغييرات الصديق)
     - "Accept Both Changes" (قبول كلاهما)
     - "Compare Changes" (مقارنة التغييرات)

2. **للملفات البسيطة مثل `index.js`:**
   - عادة تكون التغييرات نفسها
   - اختر "Accept Current Change" أو "Accept Incoming Change"
   - تأكد من إزالة جميع markers: `<<<<<<<`, `=======`, `>>>>>>>`

3. **بعد حل جميع Conflicts:**
   ```bash
   git add .
   git commit -m "Resolve merge conflicts"
   git push
   ```

### 5. أفضل Practices لتجنب Conflicts:

#### ✅ **Do:**
- Pull قبل البدء بالعمل يومياً
- Commit بشكل متكرر (كل ساعة أو ساعتين)
- Push بشكل منتظم
- تواصل مع الفريق قبل تعديل ملفات مشتركة

#### ❌ **Don't:**
- لا تترك المشروع بدون commit/push لعدة أيام
- لا تعدل ملفات `index.js` بدون سبب
- لا تضيف أسطر فارغة غير ضرورية
- لا تعدل ملفات مشتركة بدون تنسيق

### 6. استخدام `.gitattributes` (اختياري):

أنشئ ملف `.gitattributes` في جذر المشروع:

```gitattributes
# Normalize line endings
* text=auto

# Prevent conflicts in simple export files
**/index.js text eol=lf
```

### 7. نصائح إضافية:

1. **استخدم Feature Branches:**
   ```bash
   # لكل ميزة جديدة، أنشئ فرع منفصل
   git checkout -b feature/new-feature
   # اعمل التغييرات
   git add .
   git commit -m "Add new feature"
   git push origin feature/new-feature
   # ثم merge إلى main
   ```

2. **استخدم Pull Requests:**
   - بدلاً من merge مباشرة، استخدم Pull Requests
   - هذا يعطيك فرصة لمراجعة التغييرات قبل merge

3. **Regular Sync:**
   ```bash
   # جدولة يومية:
   # صباحاً: pull من main
   git checkout main
   git pull origin main
   git checkout your-branch
   git merge main  # أو git rebase main
   
   # مساءً: push تغييراتك
   git push origin your-branch
   ```

## سكريبت مساعد لحل Conflicts تلقائياً:

أنشئ ملف `fix-conflicts.sh`:

```bash
#!/bin/bash
# حل conflicts في ملفات index.js البسيطة

find src -name "index.js" -type f | while read file; do
    # إذا كان الملف يحتوي على export فقط، نظفه
    if grep -q "^export { default }" "$file"; then
        # إزالة merge conflict markers
        sed -i.bak '/^<<<<<<< HEAD$/,/^>>>>>>> origin\/j-branch$/d' "$file"
        # تنظيف الأسطر الفارغة الزائدة
        sed -i.bak '/^$/N;/^\n$/d' "$file"
        # إضافة سطر فارغ واحد في النهاية إذا لزم
        if ! tail -1 "$file" | grep -q "^$"; then
            echo "" >> "$file"
        fi
        echo "Fixed: $file"
    fi
done
```

**استخدام السكريبت:**
```bash
chmod +x fix-conflicts.sh
./fix-conflicts.sh
```

## ملخص سريع 📝

1. ✅ Pull قبل البدء بالعمل
2. ✅ Commit و Push بشكل منتظم
3. ✅ تنسيق مع الفريق قبل تعديل ملفات مشتركة
4. ✅ استخدم Feature Branches
5. ✅ حل Conflicts فوراً عند حدوثها
6. ✅ تأكد من إزالة جميع conflict markers قبل commit

---

**ملاحظة:** إذا استمرت المشكلة، فكر في استخدام Git Flow أو Trunk-Based Development كاستراتيجية للفرق الأكبر.
